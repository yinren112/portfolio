# 案例页：learn · AI学习网站（私人工具到公开产品）

> 审稿说明：【组件】标记是页面构件描述，其余是实际上屏文字。这一页是"产品化判断力"的核心案例，重点讲改造过程，不只讲功能。

---

## 页面顶部

【组件：大标题 + 一句话 + 数字条 + 在线体验按钮（醒目）】

# learn
**从一个人的私人学习工具，到所有人能用的公开网站。**

`40 课 96 题` `线上运行中` `AI 实时批改` `私人工具→公开产品`

【组件：主 CTA 按钮】
→ 在线体验：lailinkeji.com/learn/（真实功能，可直接互动，非截图）

---

## 起点：一个只有一个人用的工具

最开始做的是一个纯粹私人的命令行学习工具，压根没打算做成网站——给身边的人准备大模型和Agent方向的面试复习用的。这个工具本身就不简单：11个模块、44个知识点、108道练习题，有一套自己设计的间隔重复算法（答对后按1/3/7/14/30天逐步拉开复习间隔，答错就退一级），还有"连接题"专门考察知识能不能在不同场景之间迁移，考的是理解，不是死记硬背。

程序断网也能用——在线时用DeepSeek批改开放式回答，请求失败会自动降级成规则自评，不会让学习过程中断。整个工具打包成一个双击就能跑的exe，字体、图标、课程数据全部内置。

这个工具因为内嵌了API密钥，只能私人使用，不能公开。

---

## 决策：这个东西值得让更多人用

工具做出来、真的用出效果之后，判断是：这套"知识点分级+间隔复习+AI批改"的学习方法本身是通用的，不该锁死在一个人手里。于是写了一份完整的改造计划，把私人工具变成公开产品——这份计划本身就是这个项目最能体现工程判断力的地方。

【组件：改造清单，来自实际的TRANSFORM_PLAN.md，做成勾选列表样式】

改造前必须做到的事：
- 原私人应用完全不动，所有改造只在新目录进行，互不影响
- 新版本不能包含私人密钥、个人学习进度、私人课程内容、原有Git历史
- 公开版必须换一套全新的API密钥，不能复用私人版的
- 登录方式从"无验证"改成"8位随机学习码"——不收集手机号、邮箱、密码
- 保留核心学习体验（四级掌握、间隔复习、连接题、AI批改、断线续学）
- 首版明确不做：管理后台、排行榜、微信登录、课程上传——这些等真实使用数据证明需要时再加

最后一条尤其值得说：**主动选择不做的功能，跟做出来的功能一样重要**。

---

## 系统怎么跑

【组件：自绘SVG架构图】

```
微信小程序官网入口 (lailinkeji.com/learn/)
        ↓
Nginx（复用官网现有HTTPS证书，没有另起一套）
        ↓
Go 标准库 HTTP 服务（未引入Web框架）
  学习码鉴权 · 安全会话Cookie · 输入校验 · 日志脱敏
        ↓
SQLite（无CGO驱动，容器构建简单）
        ↓
DeepSeek API
  GradeLesson(问答判分) · GradeConnection(逻辑关系判断) · Variant(变式题生成)
  ↓ 密钥未配置时
  自动降级为规则自评，不中断学习
```

---

## 关键技术决策

### 1. 为什么后端坚持用 Go 标准库，不上框架

这是个功能边界很清楚的小应用（学习码鉴权、几个API端点、SQLite读写），不需要框架帮忙管理复杂的路由树或中间件栈。标准库自带的`net/http`已经够用，少一个框架依赖，长期维护和排查问题都更简单。

### 2. 为什么身份认证用"8位随机学习码"而不是手机号/邮箱

公开产品第一版的目标是"能学习"，不是"能做用户运营"。手机号/邮箱意味着要处理验证码、隐私合规、找回密码这些额外的工程量和合规责任。一个随机学习码既能让用户换设备恢复进度，又不涉及任何个人身份信息，边界最小。

### 3. 为什么AI批改要有自动降级

外部API不可能100%可用，密钥可能过期、请求可能超时。如果批改失败就让学习者卡住，用户体验会很差。设计上让AI批改失败时自动切换到规则自评，用户感知不到中断，只是判分精细度降低了一点。

---

## 这门课讲了什么

【组件：课程大纲选摘，做成标签云或分组列表】

零基础也能学的AI基础课，从最基本的Token讲到当前AI圈最前沿的讨论：

`Token与上下文窗口` `Temperature` `Prompt工程` `RAG五步流程` `Fine-tuning vs RAG` `MCP三大角色` `Agent运行循环(感知-推理-行动-观察)` `Multi-agent协作` `Workflow vs Agent` `数据标注与RLHF` `RLVR与测试时算力` `参差不齐的智能(Jagged Intelligence)` `上下文工程` `Scaling Laws与MoE`

这些内容是把自己长期实际使用各类AI工具积累的理解，整理成了一套可以系统学习的课程，没有照搬现成的科普文章。

---

## 数据说话

| 指标 | 数字 |
|------|------|
| 课程规模 | 40课，96道题 |
| 私人版历史 | 11模块，44知识点，108题 |
| 部署方式 | Docker Compose + Nginx，复用现有服务器 |
| 上线状态 | 线上运行中，可直接访问体验 |

---

## 技术栈一览

【组件：小标签横排】

`Go` `SQLite` `DeepSeek API` `Docker Compose` `Nginx` `原生HTML/CSS/JS`
